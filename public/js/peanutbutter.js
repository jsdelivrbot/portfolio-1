// document function global whatever
// fuck jquery man we got peanut butter
var frame = document.querySelector('.p-frame');
var count = 0;
// array picture = element.map('picture') doesn't matter
frame.addEventListener('click') {
  frame.setAttribute('src', picture[count]);
  count++;
};
var progress = ul > li
progress.aEL(){
  set all to grey
  add filled class to li[count]
};
cool cats dude
var line = document.querySelector('#hover-line');
var elements = document.querySelectorAll('.line-hover');
forEach.elements(this) {
  this.getVertical height
  and then
  line.setAttribute to the one who will rule them all
};
